package rtsd2015.tol.pm;

public class EntityTree extends Entity {

	/**
	 * Initialize entity
	 * 
	 * @param x
	 * @param y
	 */
	EntityTree(int x, int y) {
		breakable = false;
		setPos(x, y);
	}
}